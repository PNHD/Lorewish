# Representative EN sample — `en-fantasy-01`

**⚠️ NOT REAL MODEL OUTPUT.** Generated by `FakeNarrativeProvider`
(`supabase/functions/_shared/engine/fake-provider.ts`), a deterministic, template-based stand-in
used because no AI provider credential exists in this environment. This sample demonstrates that
the pipeline (context assembly → provider call → schema validation → quality gate → commit) runs
end to end and produces well-formed output shaped like a real turn — it says **nothing** about
real narrative quality, naturalness, or the "reads like natural human narrative" bar this task's
engine work exists to be judged against. See `docs/NARRATIVE_MODEL_EVALUATION.md`.

## Case setup

- Premise: *A wandering healer arrives at a village bound by a decades-old curse that silences
  anyone who speaks the founder's name.*
- Player role: a traveling healer with no memory of their own hometown
- Initial decision: *Show the guard captain the healer's satchel and ask what the village fears.*

## Output (fake provider)

> The door creaks open before you decide whether to knock. You decide: Show the guard captain the
> healer's satchel and ask what the village fears.

## Quality gate result

`schemaValid: true`, `qualityGatePassed: true`, `qualityFailures: []`, `repairRequired: false`.

## What a real run would need

`LOREWISH_NARRATIVE_PROVIDER=anthropic` + a real `ANTHROPIC_API_KEY` set as an Edge Function
secret, then `npm run bakeoff` re-run — see `docs/NARRATIVE_MODEL_EVALUATION.md` §5.
