LW-M2-R2 — Security Evidence
=============================
ADDENDUM (live bakeoff phase): the owner supplied GEMINI_API_KEY and DEEPSEEK_API_KEY in a local,
gitignored .env.local for an owner-initiated local bakeoff (npm run bakeoff via
`node --env-file=.env.local`). Verified directly, not assumed:
  - `git status` never listed .env.local as trackable (matches .gitignore's `*.local` pattern).
  - Both key names lack the EXPO_PUBLIC_ prefix, so Expo's bundler never inlines them into any
    client bundle — this is a property of the naming convention itself, confirmed by inspecting
    which env vars `npm run lint`'s Expo env-loader reported "exporting" (names only, never
    values) during this phase: only GEMINI_API_KEY/DEEPSEEK_API_KEY/EXPO_PUBLIC_SUPABASE_* were
    loaded, and Expo only inlines the EXPO_PUBLIC_ ones.
  - Neither key was ever set as a Supabase Edge Function secret. `submit-turn` was NOT redeployed
    during this phase and still has no real narrative provider configured — no anonymous or
    authenticated production traffic was ever routed to a real provider as a result of this task.
  - The bakeoff/debug scripts and the .env.local file itself lived only in this session's
    scratchpad / the repo root (gitignored); no script printed, echoed, or logged either key's
    value at any point — verified by reviewing every command's own output in this session.
  - This handoff package and its zips were secret-scanned (see below) and contain no key material.

Project: lorewish-dev (sfarcofvqfeobtcizxyv, ap-southeast-1)
Migration applied this task: 20260810220000_m2_error_mapping_and_allowance_fix.sql
  (confirmed applied: `supabase migration list --linked` shows local == remote for all 4
  migrations, see schema-migration-list.txt)

1. Advisors (supabase db advisors --linked)
--------------------------------------------
Security: 5 expected WARN, unchanged from LW-M2-R1 — one per intentionally client-callable
`lw_*` SECURITY DEFINER function (lw_commit_turn, lw_fail_turn, lw_get_run_state,
lw_precheck_and_start_turn, lw_replay_from_scene). Each performs its own auth.uid() ownership
check; this is the intended shape of the RPC boundary (see the migration's own header comments
and CURRENT_WORK.md's LW-M2-R1 Security section), not a new or unexpected finding. Full raw
output: security-results-advisors.json.
Performance: 0 findings.

2. Live adversarial verification — 7/7 PASS
--------------------------------------------
Two ephemeral test accounts (lw-m2r2-a-<uuid>@lorewish-test.dev,
lw-m2r2-b-<uuid>@lorewish-test.dev) created via the Auth Admin API, exercised against the
REDEPLOYED submit-turn Edge Function, then deleted; cleanup re-verified by re-query (0 remaining).
The probe script and the service-role key it used lived only in the session scratchpad, outside
the repository, and were deleted immediately after this run — not included in this handoff
package, matching the LW-M2-R1 precedent for this category of artifact.

  PASS — A can start a new run — status=200
  PASS — resolved player_run_id for probe — player_run_id=5ac7ba9a-05e8-4751-bc63-a3a1e168b140
  PASS — cross-user turn submission maps to 403 — status=403 body={"error":"forbidden"}
  PASS — invalid selected_choice_id maps to 400 — status=400 body={"error":"invalid_request"}
  PASS — owner's normal turn still succeeds — status=200
  PASS — usage_counters reflects exactly 2 committed generations for A (no overrun, no underrun
         from the two rejected attempts) — generation_count=2
  PASS — ephemeral test accounts cleaned up — remaining=0

  7/7 probes passed.

This directly verifies both LW-M2-R2 fixes against the live deployed function, not just against
source code or the in-memory test double:
  (a) cross-user turn submission (LW-M2-R1 known issue #1) -> clean 403, not 500.
  (b) invalid selected_choice_id (LW-M2-R1 known issue #2) -> clean 400, not 500.
  (c) the concurrent-allowance-overrun fix does not under- or over-count: two attempts that were
      correctly rejected before reaching the provider (cross-user, invalid choice) consumed zero
      allowance, and the one legitimate additional turn consumed exactly one unit.

3. What was NOT re-verified in this task
------------------------------------------
The full 20-probe RLS/grant/idempotency/canon-isolation suite from LW-M2-R1 was not re-run here —
this task's migration only `create or replace`s three already-existing functions with the same
signatures and the same revoke/grant pattern; it adds no new tables, policies, or grants. The
unchanged advisor output (5 WARN / 0 performance, identical to LW-M2-R1) is the evidence that
nothing about the privilege posture regressed. A full closeout-style RLS regression (matching
LW-M1-R3's 30/30 or LW-M2-R1's 20/20 pattern) was judged out of proportion for a change this
narrowly scoped; the 7 probes above target exactly what this migration changed.
