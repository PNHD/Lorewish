LW-M2-R2 — CI Results
========================
Pushed commits: f9d6cef, dcd4758 (feature/lw-m2-story-engine-v1 -> origin, draft PR #2 unmerged)

Push event (68469d6..dcd4758): run 31401744237 — CANCELLED by the concurrency group in favor of
  the pull_request run for the same branch, per the LW-M1-R3/LW-M2-R1 established concurrency
  design (github.workflow-github.head_ref||ref_name, cancel-in-progress: true). This is EXPECTED
  PASS behaviour per the owner's own instruction recorded in CURRENT_WORK.md's M1 section, not a
  failure — exactly one matrix runs to completion per commit.

Pull_request event: run 31401748965 — status at the time this file was first written: IN PROGRESS
  (native Android/iOS jobs typically take 20-35 minutes based on this project's prior runs; see
  CURRENT_WORK.md's M1-R3 CI section for historical timings). This file is updated below once the
  run completes, before the handoff zip is created.

--- UPDATE (no-credential phase, dcd4758) ---

Pull_request run 31401748965: SUCCESS. All 4 jobs green (Web 59s, Android 19m33s, iOS 28m54s).
This confirmed the 54-test suite (35 + 19 from that phase) on a clean GitHub-hosted runner.

--- UPDATE (live bakeoff phase — FINAL, before zip creation) ---

This phase produced a rapid series of commits (DeepSeek adapter, then four bug fixes found live,
then final docs). Each push cancelled the prior in-flight run via the project's own concurrency
group, by design (documented in .github/workflows/ci.yml as expected behaviour, not a failure) —
intermediate commits' CI runs were not individually awaited given the volume; only the FINAL
commit's own run was watched to completion, since that is the state the branch/PR actually ends
in.

Final commit: d1f74f1 (docs: LW-M2-R2 live bakeoff results). Pull_request run 31411196002:
**SUCCESS. All 4 jobs green:**
  Detect native-affecting changes .......... success,  5s
  Web / JS checks ........................... success, 1m14s
  iOS Simulator (build, install, launch) .... success, 37m16s
  Android (preview APK) ..................... success, 17m44s

Only annotations are Node.js 20 -> 24 runner-image deprecation notices and a setup-java@v4
deprecation notice — pre-existing, unrelated to this task's changes, not build failures.

URL: https://github.com/PNHD/Lorewish/actions/runs/31411196002

This confirms the final 73-test vitest suite (including all bug-fix regression tests and the
DeepSeek adapter tests added in the live bakeoff phase — all of which mock `fetch` and never call
a real provider, matching the "no provider keys, no paid calls in CI" rule) passes on a clean
GitHub-hosted runner, and that the native Android/iOS matrix is unaffected by this task's changes
(expected — every change in this task touched only supabase/functions/**, supabase/migrations/**,
and docs, none of which are native-relevant paths per the `changes` job's filter).
