LW-M2-R2 — Cost Summary (REAL LIVE DATA)
==========================================
Actual token usage returned by each provider, per the task brief's instruction — no assumed
5k/800-token estimate anywhere below. Full per-case breakdown: bakeoff-raw/*.json.
See narrative-samples/COMPARISON.md for caveats on which numbers are trustworthy.

gemini-3.5-flash-lite (12 generations, 2 full passes, fully clean)
  Total cost:            11,981 micro-dollars  ($0.011981)
  Per-generation avg:       998 micro-dollars  ($0.000998)
  Pricing used: $0.30/1M input, $2.50/1M output (docs/NARRATIVE_MODEL_EVALUATION.md §7)

deepseek-v4-pro (12 generations, 2 full passes, post thinking-mode fix)
  Total cost:            10,052 micro-dollars  ($0.010052)
  Per-generation avg:       838 micro-dollars  ($0.000838)
  Pricing used: $0.435/1M input, $0.87/1M output (§8) — no separate cache-hit rate documented
  for this tier, treated conservatively as equal to cache-miss.

deepseek-v4-flash (12 generations, 2 full passes, post thinking-mode fix)
  Total cost:             2,426 micro-dollars  ($0.002426)
  Per-generation avg:       202 micro-dollars  ($0.000202)
  Pricing used: $0.14/1M input (cache-miss), $0.0028/1M (cache-hit, applied where the API
  reported a cache split), $0.28/1M output.

gemini-3.6-flash (PARTIAL — quota-blocked; cost figures from the completed cases UNDERCOUNT
true cost — predate the thinking-token accounting fix; see notable-findings/gemini-token-
accounting-bug.md)
  Total cost (pre-fix, undercounted):  45,667 micro-dollars ($0.045667) for 12 generations
  NOT reported as a reliable cost figure for this model — a clean re-run is needed once the
  daily quota resets (SECOND_RUN_PENDING_RATE_LIMIT).

Cost per 1,000 successful Lorewish turns (extrapolated from the clean, real per-generation
averages above — NOT a 5k/800-token assumption, an extrapolation of the actual measured average):
  gemini-3.5-flash-lite:  ~$0.998
  deepseek-v4-pro:        ~$0.838
  deepseek-v4-flash:      ~$0.202
  gemini-3.6-flash:       not extrapolated — the only available figure undercounts true cost

Provider cost vs. user allowance: these are DeepSeek/Gemini provider-side costs only, not
Lorewish's own user-facing allowance economics (MVP UsageCounter, no credit system yet per
DOMAIN_MODEL.md §3) — kept separate per the task brief's instruction.
